# NexaPOS 1.0.29

- The file in the install folder used to remove NexaPOS is now clearly named `Uninstall.exe` instead of looking like a second copy of the installer.

Windows and Android releases are cumulative. Installing this release does not require installing skipped versions first.
