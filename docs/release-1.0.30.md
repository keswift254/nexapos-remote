# NexaPOS 1.0.30

- After installing an update on Windows, NexaPOS now reopens automatically instead of leaving you to relaunch it yourself.

Windows and Android releases are cumulative. Installing this release does not require installing skipped versions first.
