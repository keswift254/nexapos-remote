/// Never actually called on native (receipt_screen.dart only wires this
/// up on web) - exists so the file compiles under the same conditional
/// import as every other native/web split in this app.
void printCurrentPage() {}
